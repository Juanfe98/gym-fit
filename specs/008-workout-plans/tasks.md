# Tasks: Workout Plans

**Input**: Design documents from `specs/008-workout-plans/`
**Branch**: `008-workout-plans`
**Tests**: Not requested — validation via `npm run build` + manual 375px visual check per constitution DoD.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no shared state dependencies)
- **[Story]**: User story label (US1–US4 maps to spec.md user stories)
- Exact file paths in every description

---

## Phase 1: Setup

**Purpose**: Migration, module scaffolding, i18n keys, types, Zod schemas. No behaviour changes.

- [x] T001 Create `src/modules/workout-plans/` directory structure: `components/`, `hooks/`, `services/`, `utils/`, `types/`, `validation/` — empty dirs only; create stub `src/modules/workout-plans/index.ts` with single comment `// barrel — exports added per phase`
- [x] T002 Create `supabase/migrations/004_workout_plans.sql` — SQL from `specs/008-workout-plans/data-model.md`: CREATE TABLE workout_plans (id UUID PK, user_id UUID NOT NULL REFERENCES auth.users ON DELETE CASCADE, name TEXT NOT NULL, description TEXT, goal TEXT NOT NULL, level TEXT NOT NULL, duration_weeks INT, days_per_week INT NOT NULL, is_active BOOLEAN NOT NULL DEFAULT false, is_archived BOOLEAN NOT NULL DEFAULT false, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now()); + CREATE TABLE workout_days (id UUID PK, plan_id UUID NOT NULL REFERENCES workout_plans ON DELETE CASCADE, name TEXT NOT NULL, day_order INT NOT NULL, target_muscle_groups TEXT[] NOT NULL DEFAULT '{}'); + CREATE TABLE plan_exercises (id UUID PK, day_id UUID NOT NULL REFERENCES workout_days ON DELETE CASCADE, exercise_id TEXT NOT NULL, display_order INT NOT NULL, target_sets INT, target_reps INT, target_rep_range_min INT, target_rep_range_max INT, target_weight NUMERIC(8,2), rest_seconds INT, tempo TEXT, target_rpe NUMERIC(3,1), notes TEXT); + ENABLE ROW LEVEL SECURITY on all three tables + CREATE POLICY "Users own their plans" ON workout_plans FOR ALL USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id) + CREATE POLICY "Users own their workout days" ON workout_days FOR ALL USING (auth.uid() = (SELECT user_id FROM workout_plans WHERE id = plan_id)) WITH CHECK (same) + CREATE POLICY "Users own their plan exercises" ON plan_exercises FOR ALL USING (auth.uid() = (SELECT wp.user_id FROM workout_plans wp JOIN workout_days wd ON wd.plan_id = wp.id WHERE wd.id = day_id)) WITH CHECK (same)
- [x] T003 [P] Add 39 i18n keys to `src/i18n/ui.ts` in both `en` and `es` blocks — keys and values from `specs/008-workout-plans/data-model.md` i18n table: `navPlans`, `plansTitle`, `newPlan`, `editPlan`, `plansEmpty`, `planNameLabel`, `planGoalLabel`, `planLevelLabel`, `planDurationLabel`, `planDaysPerWeekLabel`, `planDescriptionLabel`, `planGoalMuscleGain`, `planGoalFatLoss`, `planGoalStrength`, `planGoalConditioning`, `planGoalGeneral`, `planLevelBeginner`, `planLevelIntermediate`, `planLevelAdvanced`, `planStatusActive`, `planStatusArchived`, `planFromTemplate`, `planManual`, `activatePlan`, `deactivatePlan`, `duplicatePlan`, `archivePlan`, `activationErrorNoDays`, `activationErrorNoExercises`, `addDay`, `removeDay`, `dayEditorTitle`, `planExerciseTargetSets`, `planExerciseTargetReps`, `planExerciseTargetWeight`, `planExerciseRest`, `planExerciseNotes`, `myPlans`, `createPlanCta`, `workoutHistory` (depends on T001)
- [x] T004 [P] Create `src/modules/workout-plans/types/index.ts` — import `Goal` from `@/types`; export `PlanGoal = Goal | 'general'` (inherits hyphenated values `'muscle-gain' | 'fat-loss' | 'strength' | 'conditioning'` from the shared `Goal` type plus `'general'`); export `PlanLevel`: `'beginner' | 'intermediate' | 'advanced'`; export `WorkoutPlan`, `WorkoutDay`, `PlanExercise`, `WorkoutDayWithCount` interfaces from `specs/008-workout-plans/data-model.md` TypeScript types section — use `PlanGoal` for `WorkoutPlan.goal` field type (depends on T001)
- [x] T005 [P] Create `src/modules/workout-plans/validation/plan-schema.ts` — export `planFormSchema`, `planExerciseFormSchema`, `workoutDaySchema` using Zod — schemas from `specs/008-workout-plans/contracts/workout-plans-module.md` Zod Validation Schemas section; import zod as `z`; do NOT reference `t()` in schema — use static error strings for now (depends on T001)

**Checkpoint**: Module dirs exist; migration ready to apply; i18n keys in en + es; types and schemas defined.

---

## Phase 2: Foundational — Blocking Prerequisites

**Purpose**: Services, query hooks, BottomNav update, route stubs. BLOCKS all screen work.

**⚠️ CRITICAL**: No list or detail screen can be implemented until T006–T012 are complete.

- [x] T006 Modify `src/modules/home/components/BottomNav.tsx` — (1) import `ClipboardList` from `lucide-react`; (2) add `'navPlans'` to the `NavTab.labelKey` union type; (3) remove `'history'` from the union type; (4) in `NAV_TABS` array replace `{ href: '/history', labelKey: 'history', Icon: History }` with `{ href: '/plans', labelKey: 'navPlans', Icon: ClipboardList }`; (5) remove `History` from the lucide-react import if no longer used. New order: Home → Workout → Plans → Exercises → Profile. (depends on T003 for navPlans key)
- [x] T007 [P] Create `src/modules/workout-plans/services/plans-service.ts` — imports `createClient` from `@/lib/supabase/client`; exports: `getPlans(userId: string): Promise<WorkoutPlan[]>` (SELECT all non-archived for user, order by created_at desc), `getPlansWithDayCount(userId: string): Promise<Array<WorkoutPlan & { dayCount: number }>>` (use `.select('*, workout_days(count)')` then map `plan.workout_days[0]?.count ?? 0` to `dayCount`; exclude archived), `getActivePlan(userId: string): Promise<WorkoutPlan | null>` (SELECT where is_active=true), `getPlan(planId: string): Promise<WorkoutPlan | null>`, `createPlan(userId: string, data: Omit<WorkoutPlan, 'id'|'userId'|'createdAt'|'updatedAt'>): Promise<WorkoutPlan>`, `updatePlan(planId: string, data: Partial<WorkoutPlan>): Promise<void>`, `activatePlan(userId: string, planId: string): Promise<void>` (UPDATE set is_active=false WHERE user_id=userId; then UPDATE set is_active=true WHERE id=planId), `deactivatePlan(planId: string): Promise<void>` (UPDATE set is_active=false WHERE id=planId), `archivePlan(planId: string): Promise<void>` (UPDATE set is_archived=true, is_active=false WHERE id=planId), `duplicatePlanFull(userId: string, sourcePlanId: string, days: WorkoutDay[], exercisesByDayId: Record<string, PlanExercise[]>): Promise<WorkoutPlan>` (insert new plan with name+'(copy)', is_active=false; for each day insert new workout_day on new plan; for each exercise insert new plan_exercise on corresponding new day; return new plan — full deep copy happens inside the service, not the mutation hook); all throw on Supabase error; map snake_case DB columns to camelCase type fields (depends on T004)
- [x] T008 [P] Create `src/modules/workout-plans/services/days-service.ts` — imports `createClient` from `@/lib/supabase/client`; exports: `getDays(planId: string): Promise<WorkoutDay[]>` (SELECT order by day_order asc), `getDaysWithCount(planId: string): Promise<WorkoutDayWithCount[]>` (SELECT workout_days.*, plan_exercises count — use `.select('*, plan_exercises(count)')` and map result to WorkoutDayWithCount with `exerciseCount: row.plan_exercises[0]?.count ?? 0`), `addDay(planId: string, data: Pick<WorkoutDay, 'name'|'dayOrder'|'targetMuscleGroups'>): Promise<WorkoutDay>`, `updateDay(dayId: string, data: Partial<Pick<WorkoutDay, 'name'|'dayOrder'|'targetMuscleGroups'>>): Promise<void>`, `removeDay(dayId: string): Promise<void>`; all throw on error; camelCase mapping (depends on T004)
- [x] T009 [P] Create `src/modules/workout-plans/services/exercises-service.ts` — imports `createClient` from `@/lib/supabase/client`; exports: `getPlanExercises(dayId: string): Promise<PlanExercise[]>` (SELECT order by display_order asc), `addPlanExercise(dayId: string, data: Omit<PlanExercise, 'id'|'dayId'>): Promise<PlanExercise>`, `updatePlanExercise(exerciseId: string, data: Partial<PlanExercise>): Promise<void>`, `removePlanExercise(exerciseId: string): Promise<void>`, `reorderPlanExercises(updates: Array<{id: string; displayOrder: number}>): Promise<void>` (batch upsert display_order values using `.upsert(updates.map(u => ({ id: u.id, display_order: u.displayOrder })))`); throw on error; camelCase mapping (depends on T004)
- [x] T010 [P] Create `src/modules/workout-plans/utils/template-import.ts` — imports `ROUTINES` from `@/data/routines`; imports `Goal` from `@/types`; exports `buildTemplatePayload(goal: Goal, daysPerWeek: number): Array<{ name: string; dayOrder: number; targetMuscleGroups: string[]; exercises: Array<{ exerciseId: string; displayOrder: number; targetSets: number; targetReps: number }> }>` — looks up `ROUTINES[goal]` (keys match exactly — both use hyphens e.g. `'muscle-gain'`); takes first `daysPerWeek` days from routine.days; for each day maps `focus` → name, `exercises` → array of `{ exerciseId: exercise.exerciseKey, displayOrder: index, targetSets: 3, targetReps: 10 }` (use `exerciseKey` as the exercise_id); returns array of day payloads; pure function, no Supabase calls. NOTE: `TemplatePicker` passes `Goal` (not `PlanGoal`) to this function — `'general'` is never a template option (depends on T004)
- [x] T011 [P] Create `src/modules/workout-plans/hooks/use-plans.ts` — `'use client'`; imports query hooks from `@tanstack/react-query`; imports services from plans-service, days-service, exercises-service; exports: `usePlans(userId: string | null)` with `queryKey: ['plans', 'list', userId]` calling `getPlans(userId)`, `enabled: !!userId`; `usePlan(planId: string | null)` with `queryKey: ['plans', 'detail', planId]` calling `getPlan(planId)`, `enabled: !!planId`; `usePlanDays(planId: string | null)` with `queryKey: ['plans', 'days', planId]` calling `getDaysWithCount(planId)`, `enabled: !!planId`; `usePlanExercises(dayId: string | null)` with `queryKey: ['plans', 'exercises', dayId]` calling `getPlanExercises(dayId)`, `enabled: !!dayId` (depends on T007, T008, T009, T004)
- [x] T012 Create route stub pages — create `src/app/(app)/plans/page.tsx`, `src/app/(app)/plans/new/page.tsx`, `src/app/(app)/plans/[id]/page.tsx`, `src/app/(app)/plans/[id]/edit/page.tsx`, `src/app/(app)/plans/[id]/days/[dayId]/page.tsx` — each: `'use client'`; exports default async function returning `<div className="p-4 text-gym-text">Coming soon</div>`; no imports needed. Confirms BottomNav Plans tab navigates without crashing.

**Checkpoint**: Navigate to `http://localhost:3000/plans` while authenticated → "Coming soon" renders (not 404); BottomNav shows Plans tab (not History); `npm run build` passes; other tabs unaffected.

---

## Phase 3: User Story 1 — Create a Plan (Priority: P1) 🎯 MVP

**Goal**: User can create a plan manually or from a template; plan appears in Plans List.

**Independent Test**: Navigate to `/plans` → tap "New Plan" → choose "Build manually" → fill name/goal/level/3 days → save → plan appears in list → tap "New Plan" again → choose "Start from template" → select "Muscle Gain" → set 4 days → save → new plan appears with 4 pre-built days visible on its detail screen.

- [x] T013 [P] [US1] Create `src/modules/workout-plans/components/PlanForm.tsx` — `'use client'`; props: `{ defaultValues?: Partial<PlanFormValues>; onSubmit: (v: PlanFormValues) => void; isLoading: boolean; submitLabel?: string }`; use react-hook-form + `zodResolver(planFormSchema)` from `@/modules/workout-plans/validation/plan-schema`; render: name `<input>` (label: `t('planNameLabel')`, min-h-[44px]), goal `<select>` with options for each `PlanGoal` value using t() label keys, level `<select>` with options for each `PlanLevel` value, durationWeeks `<input type="number">` optional, daysPerWeek `<input type="number" min=1 max=7>`, description `<textarea>` optional; each field shows inline error below from `formState.errors`; submit `<button disabled={isLoading}>` shows spinner when isLoading; all inputs `w-full bg-gym-surface-2 border border-gym-border rounded-lg px-3 py-2 text-gym-text`
- [x] T014 [P] [US1] Create `src/modules/workout-plans/components/TemplatePicker.tsx` — `'use client'`; import `Goal` from `@/types`; props: `{ onSelect: (goal: Goal) => void; onBack: () => void }` — uses `Goal` (not `PlanGoal`) because templates only cover the 4 routine goals (`'muscle-gain' | 'fat-loss' | 'strength' | 'conditioning'`); renders 4 card buttons for each `Goal` value; label map: `'muscle-gain'` → t('planGoalMuscleGain'), `'fat-loss'` → t('planGoalFatLoss'), `'strength'` → t('planGoalStrength'), `'conditioning'` → t('planGoalConditioning'); each card: `<button className="flex w-full items-center justify-between px-4 py-4 min-h-[44px] bg-gym-surface-2 rounded-lg border border-gym-border active:border-gym-accent">` with goal label and ChevronRight icon; back `<button>` at top left; tapping a card calls `onSelect(goal)`
- [x] T015 [P] [US1] Create `src/modules/workout-plans/components/PlanCard.tsx` — `'use client'`; props: `{ plan: WorkoutPlan; dayCount?: number; onClick?: () => void }`; renders outer `<button className="flex w-full items-center gap-3 px-4 py-3 min-h-[44px] active:bg-gym-surface-2">` with `onClick`; inside: plan name `text-gym-text text-sm font-medium`, goal + level badges `text-xs text-gym-muted bg-gym-surface-2 rounded px-2 py-0.5`, day count `text-xs text-gym-muted`, Active badge `<span className="ml-auto text-xs font-semibold text-gym-accent bg-gym-accent/10 rounded px-2 py-0.5">{t('planStatusActive')}</span>` shown only when `plan.isActive`; no archived badge needed here (archived plans filtered at list level)
- [x] T016 [US1] Create `src/modules/workout-plans/hooks/use-plan-mutations.ts` — `'use client'`; imports `useMutation`, `useQueryClient` from `@tanstack/react-query`; imports services from plans-service, days-service, exercises-service, utils/template-import; exports `usePlanMutations(userId: string | null)`: returns `{ createPlan, updatePlan }` where `createPlan` mutation payload is `{ data: PlanFormValues; templateGoal?: PlanGoal; daysPerWeek?: number }` — if `templateGoal` provided: call `createPlan` service then call `buildTemplatePayload(templateGoal, daysPerWeek)` then for each day payload: call `addDay` then for each exercise: call `addPlanExercise`; on success invalidate `['plans', 'list', userId]`; `updatePlan` mutation payload `{ planId: string; data: PlanFormValues }` — calls plans-service updatePlan; invalidates `['plans', 'detail', planId]` + `['plans', 'list', userId]`. NOTE: `activatePlan`, `deactivatePlan`, `duplicatePlan`, `archivePlan` added in later phases — use a separate `useMutation` call per action in those phases and combine in the hook's return (depends on T007, T008, T009, T010, T011)
- [x] T017 [US1] Replace `src/app/(app)/plans/new/page.tsx` stub — `'use client'`; `const { id: userId } = useUser()` (or get userId from Supabase auth via `createClient().auth.getUser()` in useEffect / TanStack Query); state: `mode: 'choose' | 'manual' | 'template'`, `selectedGoal: PlanGoal | null`; renders: when `'choose'`: two full-width buttons "Build manually" (sets mode='manual') and "Start from template" (sets mode='template') with t() labels; when `'template'`: `<TemplatePicker onSelect={(goal) => { setSelectedGoal(goal); setMode('manual') }} onBack={() => setMode('choose')} />`; when `'manual'`: `<PlanForm defaultValues={ goal: selectedGoal ?? undefined } isLoading={isPending} onSubmit={(values) => createPlan({ data: values, templateGoal: selectedGoal ?? undefined, daysPerWeek: values.daysPerWeek })} />`; on mutation success: `router.push('/plans/' + newPlan.id)`; page title `<h1 className="text-lg font-semibold text-gym-text px-4 pt-4">{t('newPlan')}</h1>` (depends on T013, T014, T016)
- [x] T018 [US1] Replace `src/app/(app)/plans/page.tsx` stub — `'use client'`; get userId from auth via the `useUserId()` helper exported from `use-plans.ts`; use `useQuery({ queryKey: ['plans', 'list', userId], queryFn: () => getPlansWithDayCount(userId!), enabled: !!userId })` directly (not `usePlans`) so each plan includes `dayCount` without N+1 queries; state: `showArchived: boolean = false`; filter: `const visiblePlans = showArchived ? plans : plans?.filter(p => !p.isArchived) ?? []`; renders: header row with `<h1>{t('plansTitle')}</h1>` + New Plan button → `/plans/new`; active plan section at top (plan where `isActive=true`) with PlanCard; list of remaining `visiblePlans` via `PlanCard` passing `dayCount={plan.dayCount}`, `onClick={() => router.push('/plans/'+plan.id)}`; empty state: when `!plans?.length`: `<p>{t('plansEmpty')}</p>` + create button; archived toggle at bottom; placeholder `{/* ActivateButton wired in T033 */}` (depends on T015, T011, T007 for getPlansWithDayCount)
- [x] T019 [US1] Replace `src/app/(app)/plans/[id]/edit/page.tsx` stub — `'use client'`; get `planId` via `useParams<{ id: string }>()`; `const { plan, isLoading } = usePlan(planId)`; `const { updatePlan, isPending } = usePlanMutations(userId)`; if `!plan && !isLoading`: render not-found state `<p>Plan not found</p>` + back button; render `<PlanForm defaultValues={mapPlanToFormValues(plan)} isLoading={isPending} onSubmit={(v) => updatePlan({ planId, data: v })} submitLabel={t('editPlan')} />`; on mutation success: `router.back()` (depends on T013, T016, T011)
- [x] T020 [US1] Update `src/modules/workout-plans/index.ts` barrel — add exports: `export { PlanCard } from './components/PlanCard'`, `export { PlanForm } from './components/PlanForm'`, `export { TemplatePicker } from './components/TemplatePicker'`, `export { usePlans, usePlan, usePlanDays, usePlanExercises } from './hooks/use-plans'`, `export { usePlanMutations } from './hooks/use-plan-mutations'`, `export type { WorkoutPlan, WorkoutDay, PlanExercise, WorkoutDayWithCount, PlanGoal, PlanLevel } from './types'`

**Checkpoint**: `/plans` renders list (may be empty). "New Plan" → manual form → save → plan in list. "New Plan" → template → Muscle Gain → 4 days → save → plan detail shows 4 pre-built days. Edit plan form works and saves.

---

## Phase 4: User Story 2 — Manage Plan Days & Exercises (Priority: P1)

**Goal**: User can add/rename/delete workout days; add/configure/reorder/remove exercises per day.

**Independent Test**: Open any plan → "Add Day" → day appears → open day editor → "Add Exercise" → search "bench press" → select → set 4 sets, 8–10 reps → save → add second exercise → tap up/down → exercises reorder → remove one → day shows single exercise. Edit day name → save → new name shown on plan detail.

- [x] T021 [P] [US2] Create `src/modules/workout-plans/hooks/use-day-mutations.ts` — `'use client'`; imports `useMutation`, `useQueryClient` from `@tanstack/react-query`; imports days-service; exports `useDayMutations(planId: string | null)`: returns `{ addDay, updateDay, removeDay, isAdding, isUpdating, isRemoving }` — `addDay` mutation: calls `days-service.addDay(planId, payload)`; on success invalidate `['plans', 'days', planId]` + `['plans', 'detail', planId]`; `updateDay` mutation: `(dayId, data)` → calls `days-service.updateDay`; invalidates same; `removeDay` mutation: `(dayId)` → calls `days-service.removeDay`; invalidates `['plans', 'days', planId]` + `['plans', 'detail', planId]` (depends on T008, T011)
- [x] T022 [P] [US2] Create `src/modules/workout-plans/hooks/use-exercise-mutations.ts` — `'use client'`; imports `useMutation`, `useQueryClient`; imports exercises-service; exports `useExerciseMutations(dayId: string | null, planId: string | null)`: returns `{ addExercise, updateExercise, removeExercise, reorderExercises }` — `addExercise(data)`: calls `exercises-service.addPlanExercise`; invalidates `['plans', 'exercises', dayId]` + `['plans', 'days', planId]`; `updateExercise(exerciseId, data)`: calls `exercises-service.updatePlanExercise`; invalidates `['plans', 'exercises', dayId]`; `removeExercise(exerciseId)`: calls `exercises-service.removePlanExercise`; invalidates `['plans', 'exercises', dayId]` + `['plans', 'days', planId]`; `reorderExercises(exercises: PlanExercise[], fromIndex: number, toIndex: number)`: compute new display_order values (swap the two items); optimistic update: `queryClient.setQueryData(['plans', 'exercises', dayId], reorderedList)`; call `exercises-service.reorderPlanExercises(updates)`; on error: `queryClient.setQueryData` back to original; on settled: invalidate `['plans', 'exercises', dayId]` (depends on T009, T011)
- [x] T023 [P] [US2] Create `src/modules/workout-plans/components/PlanDayCard.tsx` — `'use client'`; props: `{ day: WorkoutDayWithCount; onEdit: () => void; onRemove: () => void }`; renders `<div className="flex items-center gap-3 px-4 py-3 min-h-[44px]">`: day.name `text-sm font-medium text-gym-text`, exercise count badge `<span className="text-xs text-gym-muted bg-gym-surface-2 rounded px-2 py-0.5">{day.exerciseCount} exercises</span>`, edit icon button `<button onClick={onEdit} className="ml-auto min-h-[44px] min-w-[44px] flex items-center justify-center"><Pencil className="h-4 w-4 text-gym-muted" /></button>`, delete icon button `<button onClick={onRemove} className="min-h-[44px] min-w-[44px] flex items-center justify-center"><Trash2 className="h-4 w-4 text-gym-muted" /></button>`; import Pencil, Trash2 from lucide-react
- [x] T024 [P] [US2] Create `src/modules/workout-plans/components/PlanExerciseRow.tsx` — `'use client'`; props: `{ exercise: PlanExercise; exerciseName: string; canMoveUp: boolean; canMoveDown: boolean; onMoveUp: () => void; onMoveDown: () => void; onRemove: () => void; onEdit: () => void }`; renders `<div className="flex items-center gap-2 px-4 py-3 min-h-[44px]">`: exercise name `text-sm font-medium text-gym-text flex-1`, target summary `text-xs text-gym-muted` (e.g. "3 × 8–10" or "3 × 8" depending on which fields are set), ChevronUp button (disabled+opacity-30 when `!canMoveUp`), ChevronDown button (disabled+opacity-30 when `!canMoveDown`), edit button, trash button; all buttons `min-h-[44px] min-w-[44px]`; import ChevronUp, ChevronDown, Pencil, Trash2 from lucide-react
- [x] T025 [P] [US2] Create `src/modules/workout-plans/components/PlanExerciseForm.tsx` — `'use client'`; props: `{ defaultValues?: Partial<PlanExerciseFormValues>; onSubmit: (v: PlanExerciseFormValues) => void; onCancel: () => void; isLoading: boolean }`; use react-hook-form + zodResolver(planExerciseFormSchema); fields: targetSets (number, optional), targetReps (number, optional), targetRepRangeMin + targetRepRangeMax (two number inputs side-by-side, optional), targetWeight (number, optional), restSeconds (number, optional), notes (textarea, optional); labels via t() keys; save + cancel buttons both min-h-[44px]; inline field errors
- [x] T026 [US2] Create `src/modules/workout-plans/components/PlanDayList.tsx` — `'use client'`; props: `{ days: WorkoutDayWithCount[]; onAddDay: () => void; onEditDay: (dayId: string) => void; onRemoveDay: (dayId: string) => void }`; renders: if days.length === 0: `<p className="px-4 py-6 text-sm text-gym-muted text-center">No days yet</p>`; ordered list of `<PlanDayCard>` for each day; "Add Day" button at bottom `<button onClick={onAddDay} className="flex w-full items-center justify-center gap-2 min-h-[44px] border-t border-gym-border text-gym-accent text-sm font-semibold"><Plus className="h-4 w-4" />{t('addDay')}</button>`; import Plus from lucide-react (depends on T023)
- [x] T027 [US2] Create `src/modules/workout-plans/components/WorkoutDayEditor.tsx` — `'use client'`; props: `{ day: WorkoutDay; planId: string }`; `const { exercises } = usePlanExercises(day.id)`, `const { addExercise, updateExercise, removeExercise, reorderExercises } = useExerciseMutations(day.id, planId)`, `const { updateDay } = useDayMutations(planId)`; state: `editingId: string | null`, `pickerOpen: boolean`, `dayName: string = day.name`; renders: header with editable `<input value={dayName} onBlur={() => updateDay(day.id, {name: dayName})} className="text-lg font-semibold text-gym-text bg-transparent border-b border-gym-border w-full" />`; `<ExercisePicker open={pickerOpen} onClose={() => setPickerOpen(false)} onSelect={({exerciseId, exerciseNameSnapshot}) => { addExercise({ exerciseId, displayOrder: exercises.length, ...defaultTargets }); setPickerOpen(false) }} />` — import ExercisePicker from `@/modules/workout-session/components/ExercisePicker`; list of `<PlanExerciseRow>` where `exerciseName` resolved via static exercises JSON or exerciseNameSnapshot; when `editingId === exercise.id`: show `<PlanExerciseForm>` inline below the row; "Add Exercise" button at bottom calls `setPickerOpen(true)` (depends on T024, T025, T021, T022)
- [x] T028 [US2] Replace `src/app/(app)/plans/[id]/page.tsx` stub — `'use client'`; get `planId` via `useParams<{ id: string }>()`; `const { plan } = usePlan(planId)`, `const { days } = usePlanDays(planId)`, `const { addDay, removeDay } = useDayMutations(planId)`; renders: back button `<button onClick={router.back}>` + `<h1>{plan.name}</h1>` header row; goal badge + level badge; `<PlanDayList days={days ?? []} onAddDay={() => addDay({ name: 'Day ' + ((days?.length ?? 0) + 1), dayOrder: days?.length ?? 0, targetMuscleGroups: [] })} onEditDay={(dayId) => router.push('/plans/'+planId+'/days/'+dayId)} onRemoveDay={(dayId) => removeDay(dayId)} />`; placeholder `{/* ActivateButton — wired T033 */}`; placeholder `{/* PlanActions — wired T038 */}`; Edit plan link → `/plans/${planId}/edit` (depends on T023, T026, T021)
- [x] T029 [US2] Replace `src/app/(app)/plans/[id]/days/[dayId]/page.tsx` stub — `'use client'`; get `planId` + `dayId` via `useParams<{id: string; dayId: string}>()`; `const { days } = usePlanDays(planId)`, `const day = days?.find(d => d.id === dayId)`; if not found: render `<p>Day not found</p>` + back link; renders: back button + `<WorkoutDayEditor day={day} planId={planId} />`; loading skeleton while days loading (depends on T027)
- [x] T030 [US2] Update `src/modules/workout-plans/index.ts` barrel — add: `export { PlanDayCard } from './components/PlanDayCard'`, `export { PlanDayList } from './components/PlanDayList'`, `export { PlanExerciseRow } from './components/PlanExerciseRow'`, `export { PlanExerciseForm } from './components/PlanExerciseForm'`, `export { WorkoutDayEditor } from './components/WorkoutDayEditor'`, `export { useDayMutations } from './hooks/use-day-mutations'`, `export { useExerciseMutations } from './hooks/use-exercise-mutations'`

**Checkpoint**: Plan detail shows days list. "Add Day" appends a new day. Open day editor → add exercise via picker → set targets → save → exercise shown with target summary. Reorder up/down works. Remove exercise works. Edit day name saves on blur.

---

## Phase 5: User Story 3 — Activate a Plan (Priority: P1)

**Goal**: One active plan at a time; activation blocked if plan has no days or any day has no exercises.

**Independent Test**: Open plan with 0 days → "Activate" blocked (error shown). Open plan with ≥1 day with ≥1 exercise → "Activate" → Active badge appears → Plans List shows badge → activate second plan → first loses badge. Open active plan → "Deactivate" → no active plan.

- [x] T031 [US3] Create `src/modules/workout-plans/components/ActivateButton.tsx` — `'use client'`; props: `{ isActive: boolean; hasValidStructure: boolean; onActivate: () => void; onDeactivate: () => void; activationError?: string | null; isLoading: boolean }`; when `isActive`: render `<button onClick={onDeactivate} className="h-11 w-full rounded-lg border border-gym-border text-gym-muted font-semibold disabled:opacity-50" disabled={isLoading}>{t('deactivatePlan')}</button>`; when `!isActive && hasValidStructure`: render `<button onClick={onActivate} className="glow-accent h-11 w-full rounded-lg bg-gym-accent font-semibold text-white disabled:opacity-50" disabled={isLoading}>{t('activatePlan')}</button>`; when `!isActive && !hasValidStructure`: render disabled button + `<p role="alert" className="text-xs text-red-400 mt-1">{activationError}</p>` below; all variants min-h-[44px]
- [x] T032 [US3] Update `src/modules/workout-plans/hooks/use-plan-mutations.ts` — add to the returned object: `activatePlan(planId: string)` mutation calling `plans-service.activatePlan(userId, planId)`, on success invalidate `['plans', 'list', userId]` + `['plans', 'detail', planId]`; `deactivatePlan(planId: string)` mutation calling `plans-service.deactivatePlan(planId)`, invalidates same; export `isActivating`, `isDeactivating` booleans
- [x] T033 [US3] Update `src/app/(app)/plans/[id]/page.tsx` — replace `{/* ActivateButton — wired T033 */}` placeholder with: `const { activatePlan, deactivatePlan, isActivating } = usePlanMutations(userId)`; compute `const hasValidStructure = (days?.length ?? 0) > 0 && (days ?? []).every(d => d.exerciseCount > 0)`; compute `const activationError = !days?.length ? t('activationErrorNoDays') : t('activationErrorNoExercises')`; render `<ActivateButton isActive={plan.isActive} hasValidStructure={hasValidStructure} onActivate={() => activatePlan(planId)} onDeactivate={() => deactivatePlan(planId)} activationError={hasValidStructure ? null : activationError} isLoading={isActivating} />` in a sticky bottom bar `<div className="sticky bottom-16 p-4 bg-gym-surface border-t border-gym-border">` (depends on T031, T032)
- [x] T034 [US3] Update `src/app/(app)/plans/page.tsx` — refine active plan display: show `<PlanCard>` for active plan in a dedicated top section with `<h2 className="text-xs font-semibold text-gym-muted px-4 pt-4 pb-1 uppercase tracking-wide">{t('planStatusActive')}</h2>` header; remaining non-archived non-active plans under a second section; Active badge on PlanCard already renders from T015 — confirm it renders correctly
- [x] T035 [US3] Update `src/modules/workout-plans/index.ts` barrel — add: `export { ActivateButton } from './components/ActivateButton'`

**Checkpoint**: Activation guard works (blocked with correct error when structure invalid). Activating wires one plan active, deactivates previous. Active badge visible on list. Deactivate removes badge.

---

## Phase 6: User Story 4 — Duplicate & Archive (Priority: P2)

**Goal**: Deep-copy a plan; archive hides it from default list and deactivates it.

**Independent Test**: Open any plan → "Duplicate" → new plan appears in list with "(copy)" suffix, not active → edit copy → original unchanged. Open any plan → "Archive" → confirm → plan disappears from list → "Show archived" toggle → plan reappears with archived badge → active plan archived → both deactivated and hidden.

- [x] T036 [P] [US4] Create `src/modules/workout-plans/components/PlanActions.tsx` — `'use client'`; props: `{ onDuplicate: () => void; onArchive: () => void; isDuplicating: boolean; isArchiving: boolean }`; state: `confirmArchive: boolean`; renders two buttons stacked or side-by-side: "Duplicate Plan" `<button onClick={onDuplicate} disabled={isDuplicating} className="h-11 w-full rounded-lg border border-gym-border text-gym-text font-medium">`; when `!confirmArchive`: "Archive Plan" `<button onClick={() => setConfirmArchive(true)} className="h-11 w-full rounded-lg border border-red-400 text-red-400 font-medium">`; when `confirmArchive`: two buttons "Confirm archive" (calls `onArchive()`, `setConfirmArchive(false)`) and "Cancel" (`setConfirmArchive(false)`); all min-h-[44px]
- [x] T037 [US4] Update `src/modules/workout-plans/hooks/use-plan-mutations.ts` — add: `duplicatePlan(planId: string)` mutation — calls `plans-service.duplicatePlan(userId, planId)` which deep-copies plan metadata; THEN fetch source plan's days with `getDays(planId)`; for each day: call `addDay` on new plan with same name/dayOrder/targetMuscleGroups; for each day: fetch `getPlanExercises(sourceDayId)` and for each exercise: call `addPlanExercise` on corresponding new day; on success: invalidate `['plans', 'list', userId]`; `archivePlan(planId: string)` mutation — calls `plans-service.archivePlan(planId)` (service already deactivates if active); on success: invalidate `['plans', 'list', userId]` + `['plans', 'detail', planId]`; export `isDuplicating`, `isArchiving` (depends on T007, T008, T009)
- [x] T038 [US4] Update `src/app/(app)/plans/[id]/page.tsx` — replace `{/* PlanActions — wired T038 */}` placeholder with: `const { duplicatePlan, archivePlan, isDuplicating, isArchiving } = usePlanMutations(userId)`; render `<PlanActions onDuplicate={() => duplicatePlan(planId).then(() => router.push('/plans'))} onArchive={() => archivePlan(planId).then(() => router.push('/plans'))} isDuplicating={isDuplicating} isArchiving={isArchiving} />` above the ActivateButton sticky bar (depends on T036, T037)
- [x] T039 [US4] Update `src/app/(app)/plans/page.tsx` — confirm `showArchived` toggle works end-to-end: archived plans excluded from `visiblePlans` when `!showArchived`; when `showArchived = true`, archived plans shown with `<span className="text-xs text-gym-muted bg-gym-surface-2 rounded px-2">{t('planStatusArchived')}</span>` badge inside PlanCard (can pass an `isArchived` prop OR conditionally render badge in the list view); toggle button label switches between "Show archived" / "Hide archived"
- [x] T040 [US4] Update `src/modules/workout-plans/index.ts` barrel — add: `export { PlanActions } from './components/PlanActions'`

**Checkpoint**: Duplicate produces deep copy with "(copy)" suffix. Original unchanged. Archive hides plan from default list, deactivates if was active. Archived plans appear with badge when toggle enabled.

---

## Phase 7: Polish & DoD Validation

**Purpose**: Home CTA, History access from Profile, build check, visual validation.

- [x] T041 [P] Modify `src/modules/home/components/StartWorkoutCTA.tsx` — add `const { data: activePlan } = useQuery({ queryKey: ['plans', 'list', userId], ... })` or import `usePlans`; when `activePlan` is non-null: show plan name `<p className="text-sm font-medium text-gym-text">{activePlan.name}</p>` and "Start from plan" button below existing CTA; when no active plan: add secondary link `<Link href="/plans/new" className="text-sm text-gym-muted underline">{t('createPlanCta')}</Link>` below the primary start button; use `getActivePlan(userId)` from plans-service or filter from usePlans result
- [x] T042 [P] Update `src/app/(app)/profile/page.tsx` — add a "Workout History" section entry point: `<Link href="/history" className="flex items-center justify-between px-4 py-3 min-h-[44px] border-b border-gym-border"><span className="text-sm text-gym-text">{t('workoutHistory')}</span><ChevronRight className="h-4 w-4 text-gym-muted" /></Link>` so users can access history after it was removed from BottomNav (import ChevronRight from lucide-react)
- [x] T043 Run `npm run build` — verify zero TypeScript errors; fix any type errors before marking done
- [x] T044 Manual DoD check per `specs/008-workout-plans/quickstart.md` DoD Checklist: (1) Plans List: active badge, archived toggle, empty state; (2) Create manual plan: form validation + save; (3) Create from template: days + exercises pre-populated; (4) Day editor: add exercise, set targets, reorder, remove; (5) Activate guard: blocked on empty plan/day; (6) One active plan at a time; (7) Duplicate: copy with (copy) suffix, original unchanged; (8) Archive: hidden, deactivated, restored via toggle; (9) BottomNav Plans tab works, History gone from nav; (10) History accessible from Profile screen; (11) Home CTA shows active plan or create link; (12) All screens 375px no overflow; (13) `npm run build` passes; (14) Migration applied + RLS verified; (15) All 39 i18n keys in en + es

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: No dependencies — start immediately; T003–T005 parallel after T001
- **Phase 2 (Foundational)**: Depends on Phase 1 — T006–T011 parallel; T012 after T006
- **Phase 3 (US1)**: T013–T015 parallel after Phase 2; T016 after T013–T015; T017 after T013, T014, T016; T018 after T015; T019 after T013, T016; T020 after T017–T019
- **Phase 4 (US2)**: T021–T025 parallel after Phase 2; T026 after T023; T027 after T024, T025, T021, T022; T028 after T026; T029 after T027; T030 after T028, T029
- **Phase 5 (US3)**: T031 parallel with T032; T033 after T031, T032; T034 after T033; T035 after T034
- **Phase 6 (US4)**: T036 parallel; T037 after T036; T038 after T036, T037; T039 after T038; T040 after T039
- **Phase 7**: T041 + T042 parallel; T043 after all phases; T044 after T043

### Parallel Opportunities

```
Phase 1:  T001 → T002 → [T003 ‖ T004 ‖ T005]
Phase 2:  [T006 ‖ T007 ‖ T008 ‖ T009 ‖ T010] → T011 → T012
Phase 3:  [T013 ‖ T014 ‖ T015] → T016 → [T017 ‖ T018 ‖ T019] → T020
Phase 4:  [T021 ‖ T022 ‖ T023 ‖ T024 ‖ T025] → [T026 ‖ T027(after T024,T025,T021,T022)] → T028 → T029 → T030
Phase 5:  [T031 ‖ T032] → T033 → T034 → T035
Phase 6:  T036 → T037 → T038 → T039 → T040
Phase 7:  [T041 ‖ T042] → T043 → T044
```

Phases 3 and 4 can be worked in parallel after Phase 2 completes (different files, no cross-phase dependency until T028 uses T023/T026 from Phase 4 and T016/T011 from Phase 3).

---

## Implementation Strategy

### MVP (US1 + US2 only — P1 stories)
1. Phase 1 (T001–T005)
2. Phase 2 (T006–T012)
3. Phase 3 (T013–T020) — Plans List + Create
4. Phase 4 (T021–T030) — Day + Exercise editing
5. Phase 5 (T031–T035) — Activation
6. T043 build check
7. **VALIDATE**: Plans end-to-end at 375px

### Full delivery order
Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5 → Phase 6 → Phase 7

Each phase is independently testable and adds a complete user story increment.
