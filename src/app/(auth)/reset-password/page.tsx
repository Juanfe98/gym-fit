import { AuthFormWrapper, ResetPasswordForm } from '@/modules/auth'

export default function ResetPasswordPage() {
  return (
    <AuthFormWrapper>
      <ResetPasswordForm />
    </AuthFormWrapper>
  )
}
